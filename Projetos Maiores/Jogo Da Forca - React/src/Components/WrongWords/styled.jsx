import styled from 'styled-components'

const WrongKeys = styled.div`
    display: flex; 
    gap: 5px;
    color: var(--color-4);
    font-size: 1rem;
`

const Keys = styled.span`
`

export {WrongKeys, Keys}